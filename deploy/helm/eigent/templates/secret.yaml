apiVersion: v1
kind: Secret
metadata:
  name: {{ include "eigent.fullname" . }}-secrets
  labels:
    {{- include "eigent.labels" . | nindent 4 }}
type: Opaque
data:
  master-key: {{ .Values.registry.masterKey | b64enc | quote }}
