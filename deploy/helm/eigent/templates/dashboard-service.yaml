apiVersion: v1
kind: Service
metadata:
  name: {{ include "eigent.fullname" . }}-dashboard
  labels:
    {{- include "eigent.dashboard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.dashboard.service.type }}
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eigent.dashboard.selectorLabels" . | nindent 4 }}
